// Define enums before usage
export enum RowAlignment {
  START = 'start',
  CENTER = 'center',
  END = 'end',
  BETWEEN = 'between',
  AROUND = 'around',
  EVENLY = 'evenly',
}

export enum RowVerticalAlignment {
  START = 'start',
  CENTER = 'center',
  END = 'end',
  BASELINE = 'baseline',
  STRETCH = 'stretch',
}
