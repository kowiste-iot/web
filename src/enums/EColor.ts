import { colors } from "@/plugins/theme/colors"


// Define a string enum for the colors
export enum EColor {
    Primary = 'primary',
    Secondary = 'secondary',
    Success='success',
    Danger = 'danger',
    Waring = 'warning',
  }
